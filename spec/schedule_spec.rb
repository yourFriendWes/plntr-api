require 'spec_helper'

describe Schedule do
  it "tells which plant it belongs to" do
    # test_plant = Plant.create({})
    # test_zone = Zone.create({})
    # test_schedule = Schedule.create({})
    # expect(test_schedule.plant()).to(eq(test_plant))
  end

  it "tells which zone it belongs to" do
    # test_plant = Plant.create({})
    # test_zone = Zone.create({})
    # test_schedule = Schedule.create({})
    # expect(test_schedule.zone()).to(eq(test_zone))
  end
end
